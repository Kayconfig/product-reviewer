{
    "name": "review-summarizer",
    "type": "module",
    "private": true,
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "workspaces": [
        "packages/*"
    ],
    "dependencies": {
        "concurrently": "^9.2.1",
        "husky": "^9.1.7",
        "lint-staged": "^16.1.5",
        "prettier": "^3.6.2"
    },
    "scripts": {
        "prepare": "husky",
        "dev": "bun run index.ts",
        "test": "bun test --forceExit --runInBand"
    }
}
